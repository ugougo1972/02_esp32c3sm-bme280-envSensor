# 03_要求仕様(03_REQUIREMENTS)

## 動作時間

06:00 ～ 23:00（17時間）  
充電なしで連続稼働

### 動作時間補足

- 電池容量は現時点で最終固定しない
- LiPo 1000mAh は初期評価用の暫定容量として扱う
- 運用途中で容量を増減できる実装方針を優先する
- 完成まで電流測定は実施せず、電池寿命の最終評価は完成後に行う

## 記録周期

1分

### 周期動作試験補足

- 試験用として 5秒周期 periodic logger を用いる
- Deep Sleep 周期起床試験では 60秒周期および30秒周期を確認済み
- Deep Sleep 周期起床時は、起床ごとに 1回測定して 1行CSV追記する構成を基準とする

## 記録方式

microSDへCSV保存

### 記録系補足

- 初期検証では `/log.csv`、`/log_env.csv`、`/log_env_loop.csv`、`/log_sleep_env.csv` を試験用ファイルとして扱う
- 実運用時は列順を固定し、再起動後も追記可能であること
- 旧形式の試験ファイルと実運用ファイルを混在させないこと
- Deep Sleep 周期起床試験では `timestamp,temp_c,hum_pct,press_hpa,als,uvs,boot_count,wakeup_cause` 形式を確認済み

## 省電力方針

- Wi-Fi OFF
- Bluetooth OFF
- DeepSleep使用
- OLED常時消灯
- 測定・保存後すぐSleep

### Deep Sleep 補足

- Deep Sleep 復帰後は毎回必要な初期化をやり直す前提とする
- USB CDC の COM ポートは Deep Sleep 中に切断され得るため、Deep Sleep 試験の合否は CSV 追記結果を優先して判定する
- Deep Sleep 実消費電流は完成まで未測定とし、別途最終評価で扱う

## ユーザー入力

RGB LED付きスイッチ付きロータリーエンコーダ

### 入力内容

- 移動ログ
- 頭痛ステータス
- 基本画面遷移操作

### 入力系補足

- 右回転を CW、左回転を CCW とする
- 5ノッチ = 5イベントを成立条件とする
- PUSH / RELEASE を区別して扱う
- RGB LED は外付け電流制限抵抗を前提とする
- LED は共通アノード前提を第一候補とする

## 表示

OLED 0.96インチ

- 操作時のみ表示
- 測定後5秒表示
- メイン画面では以下を優先表示する
  - 温度
  - 湿度
  - 気圧
  - ALS
  - UV
- 現行試作レイアウトでは以下の並びを基本とする
  - 1行目: 温度 / 湿度
  - 2行目: 気圧
  - 3行目: ALS / UV
- メニューは常時表示ではなく、押下時に別画面として表示する
- 上段には時刻欄と電池アイコンの配置余地を持たせる
- センサー測定数値の文字サイズ・フォントは統一方針とする
- 128x64 OLED のまま UI 改善を優先し、128x128化やカラー化は後段検討とする

## センサ要件

### BME280

- 温度測定が可能であること
- 湿度測定が可能であること
- 気圧測定が可能であること
- 3.3V系で動作すること
- I2C接続可能であること

### LTR390

- ALS（照度系指標）が取得可能であること
- UVS（UV系指標）が取得可能であること
- 3.3V系で動作すること
- I2C接続可能であること
- 室内低UV環境で0付近を取り得ることを許容する
- ALS / UVS 切替直後の一時的な0を異常扱いしない
- ALS / UVS 切替を含む連続ログ試験で値が固定化しないこと
- ALS / UVS 切替時は新データ待ちと安定待ち時間を考慮すること
- Deep Sleep 周期起床試験でも ALS / UVS の再取得が継続すること

### OLED

- I2C接続で文字表示が可能であること
- センサ値の複数項目同時表示が可能であること
- 操作用メニュー表示に使用可能であること
- 時刻欄および電池アイコンの表示試作が可能であること

## RTC要件

### DS3231

- I2C接続可能であること
- 現在日時を保持できること
- 主電源断後もバックアップ条件下で時刻保持できること
- 通常運用時の正時刻源として利用できること
- PCからUSBシリアル経由で時刻設定できること
- インターネット時刻同期はPC側で実施し、ESP32は受信日時をDS3231へ反映する方式とする
- `lostPower()` 検出時に時刻再設定できること
- 2000/01/01 などの不正時刻を検出できること
- 試験時は不正時刻検出後に自動再設定できること
- Deep Sleep 周期起床時の時刻源として利用できること

## 記録系要件

### microSD

- SPI接続で初期化できること
- テキストファイルの新規作成、読出し、追記ができること
- CSVファイルのヘッダ作成とデータ行追記ができること
- 再起動後も既存CSVへ追記できること
- RTC時刻付きCSV保存ができること
- BME280 / LTR390 を含む6列CSV保存ができること
- 5秒周期程度の試験用連続ロガーで継続追記できること
- Deep Sleep 周期起床でも毎周期 `SD.begin()` と追記が成立すること

## 電源要件

### 最小電源経路

- LiPo → TP4056 → XC9306 → XIAO ESP32S3 Plus の給電経路が成立すること
- LiPo駆動で XIAO ESP32S3 Plus が起動し、最小スケッチを実行できること
- TP4056 充電入力時に異常発熱・異臭がないこと
- XC9306 出力で 3.3V 系が安定すること

### 電源実装方針

- 初号機では USB通信用USB と LiPo充電用USB を分離する
- 初号機では電源回りを別基板化することを第一候補とする
- Li-Po ←→ TP4056 は JST コネクタ接続を前提とする
- 電源基板 ←→ メイン基板は端子台経由ケーブル接続を前提とする
- 電源基板は LiPo / TP4056 / XC9306 を基本範囲とする
- 分圧回路はメイン基板側に実装することを第一候補とする

### battery voltage monitor 要件

- `TP4056 OUT+ / OUT-` を測定対象とする
- XIAO の ADC対応ピンで読めること
- ADC入力には分圧回路を入れること
- 初期値は `100kΩ + 100kΩ` の 1:1 分圧を第一候補とする
- 必要に応じて ADC中点-GND 間へ安定化用コンデンサーを追加できること
- `vbat` の値はテスター実測と概ね整合すること

## MCU運用方針

- 最終構成の主候補MCUは XIAO ESP32S3 Plus とする
- センサ・表示の評価は ESP32-WROOM系開発ボードでも実施可能とする
- 評価用MCUと最終MCUでピン定義は分けて管理する
- XIAO ESP32S3 Plus のUSBシリアル確認では Arduino IDE の `USB CDC On Boot = Enabled` を前提条件として扱う

## MCU変更による消費電力前提の変更

MCUを ESP32-C3 系想定から ESP32-S3 系主候補へ見直したため、  
DeepSleep時消費電流の前提値は再評価対象とする。

- ESP32-C3 DeepSleep: 約10〜20µA（参考値）
- ESP32-S3 DeepSleep: 約20〜50µA（参考値）

### 変更理由

- 電池寿命計算に影響するため
- 実ボード差、周辺回路差の影響が大きいため
- 評価用MCUと最終MCUを分けて確認する必要があるため

※ 実測値は基板・周辺回路・電源方式に依存するため、完成後に実測する。

## 初号機実装方針

- 初号機はユニバーサル基板ベースとする
- センサ、RTC、記録、電源は既製モジュール活用を前提とする
- 電池は交換・容量変更しやすい構成を優先する
- 専用基板化は初号機とは分離して後段で検討する

## XIAO ESP32S3 Plus 単体要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| USB接続認識 | 必須 | OK |
| スケッチ書込み | 必須 | OK |
| シリアル出力 | 必須 | OK |
| 基本GPIO出力 | 必須 | OK |
| 再接続時の安定認識 | 必須 | OK |

結論: XIAO ESP32S3 Plus は、単体Bring-upに必要な最小要件を満たすことを確認した。

## BME280 要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| 温度測定 | 必須 | OK |
| 湿度測定 | 必須 | OK |
| 気圧測定 | 必須 | OK |
| I2C通信 | 必須 | OK |
| 3.3V動作 | 必須 | OK |
| 連続測定 | 必須 | OK |

結論: BME280 は本プロジェクトの環境センサ要件を満たすことを確認した。

## LTR390 要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| ALS測定 | 必須 | OK |
| UVS測定 | 必須 | OK |
| I2C通信 | 必須 | OK |
| 3.3V動作 | 必須 | OK |
| 連続測定 | 必須 | OK |
| 周囲変化追従 | 必須 | OK |
| 切替後安定読出し | 必須 | OK |
| Deep Sleep後の再読出し | 必須 | OK |

補足:

- 単体試験では明暗変化への追従を確認
- periodic logger 初版では ALS 固定問題を確認
- ALS / UVS 切替ごとの安定待ちを入れた修正版で解消
- Deep Sleep 周期起床試験でも ALS が広範囲に変化し、固定化しないことを確認

結論: LTR390 は本プロジェクトの照度・UV系要件を満たすことを確認した。

## OLED 要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| I2C通信 | 必須 | OK |
| 初期化 | 必須 | OK |
| 文字表示 | 必須 | OK |
| 複数項目表示 | 必須 | OK |
| メニュー別画面表示 | 必須 | OK |
| 時刻欄・電池アイコン試作 | 必須 | OK |

結論: OLED SSD1306 は本プロジェクトの表示要件の初期段階を満たすことを確認した。

## I2C統合要件検証結果（評価用MCU）

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| BME280 / LTR390 / OLED 同時接続 | 必須 | OK |
| 3デバイス同時認識 | 必須 | OK |
| 3デバイス同時初期化 | 必須 | OK |
| 3デバイス同時読出し | 必須 | OK |
| OLED同時表示 | 必須 | OK |

結論: 評価用MCU上で、BME280 / LTR390 / OLED のI2C統合要件を満たすことを確認した。

## XIAO ESP32S3 Plus I2C統合要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| BME280 / LTR390 / OLED 同時接続 | 必須 | OK |
| XIAO上での I2C再構成 | 必須 | OK |
| 3デバイス同時初期化 | 必須 | OK |
| センサー読出し継続 | 必須 | OK |
| OLED同時表示 | 必須 | OK |

結論: XIAO ESP32S3 Plus 上でも、BME280 / LTR390 / OLED のI2C統合要件を満たすことを確認した。

## DS3231 要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| I2C通信 | 必須 | OK |
| 時刻読出し | 必須 | OK |
| 任意時刻書込み | 必須 | OK |
| 再起動後保持 | 必須 | OK |
| PCシリアル経由設定 | 必須 | OK |
| CLOCK画面反映 | 必須 | OK |
| `lostPower()` 検出 | 必須 | OK |
| 妥当性判定 | 必須 | OK |
| 自動再設定（試験用） | 必須 | OK |
| Deep Sleep 時刻源 | 必須 | OK |

補足:

- DS3231本体は 0x68、付属AT24C32は 0x57 を認識
- `lostPower()` が YES の場合、2000/01/01 へ戻る状態を確認
- 固定時刻書込みおよび PCシリアル経由設定で復旧可能
- 主電源断後のバックアップ保持は別途確認継続

結論: DS3231 は本プロジェクトの RTC 要件の初期段階を満たすことを確認した。

## microSD 要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| SPI初期化 | 必須 | OK |
| カード認識 | 必須 | OK |
| テキスト新規作成 | 必須 | OK |
| テキスト読出し | 必須 | OK |
| テキスト追記 | 必須 | OK |
| CSVヘッダ作成 | 必須 | OK |
| CSVデータ追記 | 必須 | OK |
| 再起動後追記 | 必須 | OK |
| Deep Sleep後の再初期化 | 必須 | OK |

補足:

- `/test.txt` の新規作成 / 読出し / 追記を確認
- `/log.csv` のヘッダ作成と連続追記を確認
- `SD.begin` 失敗時は CS High → SPI.begin → 低速初期化で安定化した

結論: microSD は本プロジェクトの記録系要件の初期段階を満たすことを確認した。

## RTC + microSD 要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| timestamp,message 保存 | 必須 | OK |
| RTC時刻付きCSV保存 | 必須 | OK |
| 再実行時の連続追記 | 必須 | OK |

結論: RTC を時刻源とした最小ログ保存要件を満たすことを確認した。

## RTC + BME280 + microSD 要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| timestamp,temp_c,hum_pct,press_hpa 保存 | 必須 | OK |
| 実測値の時刻付き保存 | 必須 | OK |
| 不正時刻検出後の保存継続 | 必須 | OK |

補足:

- 復旧前に保存された 2000/01/01 の 1 行は無効データとして扱う

結論: BME280 実測値の時刻付きCSV保存要件を満たすことを確認した。

## RTC + BME280 + LTR390 + microSD 要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| 6列CSV保存 | 必須 | OK |
| ALS / UVS を含む保存 | 必須 | OK |
| 連続追記 | 必須 | OK |
| 5秒周期 periodic logger | 必須 | OK |
| Deep Sleep周期起床ロガー | 必須 | OK |

補足:

- `/log_env.csv` および `/log_env_loop.csv` を用いて 6列保存を確認
- periodic logger 修正版では ALS 固定問題が解消した
- UVS は室内では 0〜1 中心を許容する
- `/log_sleep_env.csv` を用いた 60秒周期および30秒周期の Deep Sleep 周期起床ロガー試験に成功した

結論: RTC + BME280 + LTR390 + microSD の最小統合、試験用連続ロガー、および Deep Sleep 周期起床ロガー要件を満たすことを確認した。

## RGB LED付きスイッチ付きロータリーエンコーダ要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| 時計回り検出 | 必須 | OK |
| 反時計回り検出 | 必須 | OK |
| 5ノッチ = 5イベント | 必須 | OK |
| 押し込み検出 | 必須 | OK |
| 解放検出 | 必須 | OK |
| RGB LED 制御 | 必須 | OK |

補足:

- XIAO ESP32S3 Plus 上で ESP32Encoder + PCNT 方式を採用
- 右回転 = CW、左回転 = CCW を確認
- PUSH / RELEASE を正常確認
- LED は共通アノード前提で扱う案を第一候補とする

結論: RGB LED付きスイッチ付きロータリーエンコーダの基本要件は満たすことを確認した。

## XIAO ESP32S3 Plus メニューUI要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| 通常画面からメニュー遷移 | 必須 | OK |
| メニューカーソル移動 | 必須 | OK |
| CLOCK / LOG / SLEEP 遷移 | 必須 | OK |
| センサー読出し継続中の操作 | 必須 | OK |
| 実機調整による操作感最適化 | 必須 | OK |

補足:

- 現行仮配線は D0(GPIO1)=Encoder A, D1(GPIO2)=Encoder B, D3(GPIO4)=Encoder SW とする
- LED 制御候補は D11(GPIO38)=R, D12(GPIO39)=G, D13(GPIO40)=B とする
- 現時点では VIEW / MENU / CLOCK / LOG / SLEEP の基本遷移が成立している

結論: XIAO ESP32S3 Plus 上での簡易メニューUI試験は成立した。

## 電源系要件検証結果

| 要件 | 内容 | 検証結果 |
|------|------|----------|
| LiPo 単体確認 | 必須 | OK |
| TP4056 端子確認 | 必須 | OK |
| TP4056 充電動作 | 必須 | OK |
| XC9306 3.3V 出力 | 必須 | OK |
| XIAO 3V3 供給 | 必須 | OK |
| LiPo駆動 Blink | 必須 | OK |

補足:

- LiPo 単体で外観異常なし、約4.0Vを確認
- TP4056 の B / OUT / IN 端子役割を確認済み
- XC9306 O-G で 3.290V 安定を確認
- XIAO 3V3-GND で 3.288V 安定を確認
- LiPo → TP4056 → XC9306 → XIAO 構成で Blink 正常動作を確認
- 発熱・異臭ともに認めず

結論: 初号機の最小電源経路要件を満たすことを確認した。

追記：PCクラッシュによりGit等再構築